/**
 * apiRequests/
 * 
 * Nơi tập trung định nghĩa tất cả các hàm gọi API tới Backend.
 * Mỗi file đại diện cho 1 module/entity (ví dụ: authApi.ts, productApi.ts).
 * 
 * Ví dụ:
 * export const getProducts = async () => { ... }
 * export const getProductById = async (id: string) => { ... }
 */
