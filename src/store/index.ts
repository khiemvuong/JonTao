/**
 * store/
 * 
 * Quản lý trạng thái toàn cục (Global State Management).
 * Sử dụng Zustand, Redux, hoặc Context API.
 * 
 * Ví dụ với Zustand:
 * export const useCartStore = create((set) => ({
 *   items: [],
 *   addItem: (item) => set((state) => ({ items: [...state.items, item] })),
 * }));
 */
