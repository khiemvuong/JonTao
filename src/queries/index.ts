/**
 * queries/
 * 
 * Chứa các function và custom hooks cho data fetching / caching.
 * Sử dụng React Query (Tanstack Query) gọi xuống tầng apiRequests.
 * 
 * Ví dụ:
 * export const useProducts = () => useQuery({ queryKey: ['products'], queryFn: getProducts });
 */
